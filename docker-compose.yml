version: "3.9"

services:
  agent:
    build: .
    container_name: smol_agent
    ports:
      - "7860:7860"
    environment:
      - HF_TOKEN=${HF_TOKEN}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./app:/app
